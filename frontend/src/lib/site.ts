/**
 * Esta versão do portfólio está publicada como arquivo, em hospedagem estática:
 * não há backend junto dela. Tudo que dependia de servidor — o formulário de
 * contato e a API de conteúdo — vive na versão atual, e é para cá que os avisos
 * mandam quem chega.
 *
 * O endereço fica em um lugar só para o aviso do topo e o do formulário nunca
 * discordarem entre si.
 */
export const CURRENT_SITE_URL = "https://michellycruz.com.br";

/** Como o endereço aparece escrito na tela, sem o protocolo. */
export const CURRENT_SITE_LABEL = "michellycruz.com.br";
